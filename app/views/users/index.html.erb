<div class="container">

  <div class="row">
    <div class="col-md-6 col-md-offset-3 panel panel-default">
<p id="notice"><%= notice %></p>

<h1 class="margin-base-vertical text-center">My Data</h1>

<table class="table table-striped table-condensed table-hover">
  <thead>
    <tr>
      <th>Name</th>
      <th>Sex</th>
      <th>Date of birth</th>
      <th>Location</th>
      <th>Score</th>
      <th colspan="5"></th>
    </tr>
  </thead>

  <tbody>
      <tr>
        <td><%= @users.first.name %></td>
        <td><%= @users.first.sex %></td>
        <td><%= @users.first.dob %></td>
        <td><%= @users.first.location %></td>
        <td><%= @users.first.score %></td>
      </tr>
  </tbody>
</table>

<br>
</div>

  </div>
</div>